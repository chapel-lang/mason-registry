os:
  - linux

matrix:
  include:
    - name: "checkTomls"
      env: TEST_COMMAND="./util/checkTomls.bash"

  include:
    - name: "make chapel"
      env: TEST_COMMAND="./util/makeChapel.bash"

script:
  - (eval "$TEST_COMMAND")
